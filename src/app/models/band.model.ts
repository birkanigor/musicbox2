import { SafeUrl } from '@angular/platform-browser';
export default class BandModel{
    constructor(public name: string, public image: string , public id: string , public src: string , public rating: number){}
}

